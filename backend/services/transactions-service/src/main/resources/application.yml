spring:
  application:
    name: transactions-service
  datasource:
    url: jdbc:h2:mem:transactions;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8082
management:
  endpoints:
    web:
      exposure:
        include: health,info
