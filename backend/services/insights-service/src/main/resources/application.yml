spring:
  application:
    name: insights-service
  datasource:
    url: jdbc:h2:mem:insights;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8085
management:
  endpoints:
    web:
      exposure:
        include: health,info
