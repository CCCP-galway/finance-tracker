spring:
  application:
    name: reporting-service
  datasource:
    url: jdbc:h2:mem:reporting;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8086
management:
  endpoints:
    web:
      exposure:
        include: health,info
