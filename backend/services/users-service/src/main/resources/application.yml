spring:
  application:
    name: users-service
  datasource:
    url: jdbc:h2:mem:users;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include: health,info
